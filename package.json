{
	"name": "vitalis-2.0",
	"version": "1.0.0",
	"description": "**Vitalis CLI** is a minimal, single-file terminal chat application that interacts with a local Ollama server running `gpt-oss-20b`. It streams tokens in real-time, operates fully offline (localhost only), and includes a safety layer for a medical-aid assistant role-play (non-diagnostic).",
	"main": "index.js",
	"scripts": {
		"dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
		"dev:backend": "./start-backend.sh",
		"dev:frontend": "cd frontend && npm run dev",
		"install:all": "npm install && cd frontend && npm install",
		"build": "cd frontend && npm run build",
		"start": "npm run dev",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Sam-T-G/Vitalis-2.0.git"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"type": "commonjs",
	"bugs": {
		"url": "https://github.com/Sam-T-G/Vitalis-2.0/issues"
	},
	"homepage": "https://github.com/Sam-T-G/Vitalis-2.0#readme",
	"devDependencies": {
		"concurrently": "^9.2.1",
		"npm-run-all": "^4.1.5"
	}
}
